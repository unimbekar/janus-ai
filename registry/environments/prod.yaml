# Production.
#
# Intentionally empty: no model is servable in production until a real provider
# deployment is reviewed and added here. An empty catalog makes the gateway
# report itself unready rather than quietly serving a mock model to a paying
# customer. Frontier YAML exists; enabling a key here is the production gate.
environment: prod

enabled_deployments: []
