# Shared development environment on AWS.
#
# Only the deterministic backends are enabled today. Frontier cloud deployments
# stay off until credentials are reviewed into this overlay. Janus GPU
# deployments land in Phase 8.
environment: dev

enabled_deployments:
  - mock-small-local
  - mock-reasoning-private
